import { AlpamatchDirective } from './alpamatch.directive';

describe('AlpamatchDirective', () => {
  it('should create an instance', () => {
    const directive = new AlpamatchDirective();
    expect(directive).toBeTruthy();
  });
});
